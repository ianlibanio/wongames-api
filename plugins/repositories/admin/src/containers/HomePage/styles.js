import styled from 'styled-components'
import { Toggle as Base } from '@buffetjs/core'

export const Wrapper = styled.div`
  padding: 18px 30px;

  p {
    margin-top: 1rem;
  }
`;

export const Toggle = styled(Base)`
  margin-bottom: 1rem;
`;
